<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Download Our App</title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-100 text-gray-800 flex flex-col items-center justify-center">
  
   <% if (success.length > 0) { %>
  <div id="alertBox" class="bg-green-500 font-bold text-white mb-4 capitalize px-4 py-2"><%= success[0] %></div>
<% } %>

  <% if (error.length > 0) { %>
  <div id="alertBox" class="bg-red-500 font-bold text-white mb-4 capitalize px-4 py-2"><%= error[0] %></div>
<% } %>

  <div class="min-h-screen flex flex-col items-center justify-center p-6">
    <div class="text-center flex-col flex gap-5">
      <h1 class="text-2xl md:text-5xl font-bold mb-4 text-blue-600">Download InvestFlipKart App</h1>
      <p class="text-lg mb-6 max-w-xl mx-auto">
        Experience the best features on the go! Available on Android   
      </p>

      <img src="../../images/app1.jpg" alt="App Screenshot" class="w-full max-w-md rounded-xl shadow-lg" />
      <img src="../../images/app2.jpg" alt="App Screenshot" class="w-full max-w-md rounded-xl shadow-lg" />
      <img src="../../images/app3.jpg" alt="App Screenshot" class="w-full max-w-md rounded-xl shadow-lg" />
        <div class="flex gap-4 w-full justify-center mb-8">
       
        <a href="/investflipkart.apk" class="bg-yellow-600 text-yellow-200 px-6 py-3 rounded-lg flex hover:text-yellow-800 items-center text-lg flex-col font-bold gap-2 hover:bg-green-400 transition">
          <img src="../../images/flipcart.png" class="w-12 h-12" alt="Android">
          <span>Download now</span>
        </a>
      </div>
    </div>
  </div>

<script>
   setTimeout(() => {
    const alertBox = document.getElementById('alertBox');
    if (alertBox) {
      alertBox.style.opacity = '0';
      setTimeout(() => alertBox.remove(), 1000); // Remove from DOM after fade out
      
    }
  }, 5000); // 3 seconds 
</script>
</body>
</html>
