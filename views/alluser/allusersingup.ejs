<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Create Account</title>
  <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
</head>
<body class="bg-yellow-400 min-h-screen flex flex-col items-center justify-center">
  <% if (success.length > 0) { %>
  <div id="alertBox" class="bg-green-500 font-bold text-white mb-4 capitalize px-4 py-2"><%= success[0] %></div>
<% } %>

  <% if (error.length > 0) { %>
  <div id="alertBox" class="bg-red-500 font-bold text-white mb-4 capitalize px-4 py-2"><%= error[0] %></div>
<% } %>

 
  <div class="w-full max-w-md p-6">
    
    <!-- Logo -->
    <div class="flex justify-center mb-6">
      <img src="../../images/flipcart.png" alt="Logo" class="w-24 h-24 rounded-full shadow-lg" /> 
    </div>

    <!-- Form -->
    <form action="/authe/alluser" method="POST" class="space-y-4">
      <input type="text" name="name" placeholder="Name" class="w-full px-4 py-3 rounded-md bg-white text-gray-700 placeholder-gray-400 focus:outline-none"  >
      
      <input type="number" name="phone" placeholder="Mobile Number" class="w-full px-4 py-3 rounded-md bg-white text-gray-700 placeholder-gray-400 focus:outline-none"  >

      <input type="password" name="password" placeholder="Password" class="w-full px-4 py-3 rounded-md bg-white text-gray-700 placeholder-gray-400 focus:outline-none"  >

      <input type="password" name="confirmPassword" placeholder="Confirm Password" class="w-full px-4 py-3 rounded-md bg-white text-gray-700 placeholder-gray-400 focus:outline-none"  >

      <input type="text" name="otp" placeholder="OTP" class="w-full px-4 py-3 rounded-md bg-white text-gray-700 placeholder-gray-400 focus:outline-none"  >

      <input type="text" value="<%=whoinvite%>" name="whoinvitecode" placeholder="Invite Code (optional)" class="w-full px-4 py-3 rounded-md bg-white text-gray-700 placeholder-gray-400 focus:outline-none">
      <input type="text" value="web" hidden name="web"   class="w-full px-4 py-3 rounded-md bg-white text-gray-700 placeholder-gray-400 focus:outline-none">
 
      <button type="submit"   class="w-full py-3 rounded-md bg-blue-700 text-white font-bold hover:bg-blue-800 transition">Create Account</button>
    </form>
  </div>
  
  <script>
   setTimeout(() => {
    const alertBox = document.getElementById('alertBox');
    if (alertBox) {
      alertBox.style.opacity = '0';
      setTimeout(() => alertBox.remove(), 1000); // Remove from DOM after fade out
      
    }
  }, 5000); // 3 seconds 
</script>
</body>
</html>
