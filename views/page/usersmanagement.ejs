<div class="bg-purple-50 text-gray-800 max-w-[100vh] overflow-scroll">
  <div class="max-w-6xl mx-auto py-10">
    <h1 class="text-3xl font-bold text-purple-700 mb-8">User Management</h1>

    <table class="min-w-full bg-white shadow-md  overflow-hidden">
      <thead class="bg-purple-600 text-white">
        <tr>
          <th class="py-1 px-3 text-center">#</th>
          <th class="py-1 px-3 text-center">Name</th>
          <th class="py-1 px-3 text-center">Phone</th>
          <th class="py-1 px-3 text-center">Total_Recharge</th>
          <th class="py-1 px-3 text-center">Total_Withdrawal</th>
          <th class="py-1 px-3 text-center">Actions</th>
        </tr>
      </thead>
      <tbody>
        <% users.forEach((user, index) => { %>
          <tr class="border-b text-[12px] hover:bg-purple-100">
            <td class="py-1  text-center px-3"><%= index + 1 %></td>
            <td class="py-1  text-center px-3"><%= user.name %></td>
            <td class="py-1 text-center px-3"><%= user.phone %></td>
            <td class="py-1 text-center px-3"><%= user.total_recharge %></td>
            <td class="py-1 text-center px-3"><%= user.total_withdrawal %></td>
           
            <td class="py-1 items-center justify-center flex px-3"> 
              <button class="bg-black hover:bg-gray-600 text-white px-4 py-1 rounded-md text-sm">Block</button>
            </td>
          </tr>
        <% }); %>
      </tbody>
    </table>
  </div>
</div>

  